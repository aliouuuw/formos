Formos — project progress log
==============================

2026-06-11 (checkpoint)
-----------------------
- Visual form builder shipped: drag-and-drop fields (@dnd-kit), Typeform-style 3-pane editor (structure / canvas / inspector).
- Editor polish: sidebar nav, inspector layout, live preview refinements, contrast pass (COLOR-* fixes).
- UX fixes: progress bar semantics, unsaved-changes guard, save feedback, slug/view-live consistency, analytics dedup.
- Backlog: builder-ui + many bug/UX items marked done; 9 todo items remain (embeds, webhooks, conditional logic, etc.).
- Git: main @ 664db8f, working tree clean.
- Review (Bugbot + Security): no branch diff on main — subagents could not run; prior findings in context/review-findings.json (all BUG/SEC items remediated).

Next up (priority order)
-------------------------
1. ~~Builder preview ↔ FormRenderer alignment (UX-001, UX-034)~~ — done.
2. ~~Slug change redirect / warning~~ — done (form_slug_redirects + editor banner).
3. Duplicate submission guard + spam honeypot (lead pipeline hygiene).
4. Embeddable forms (backlog embed-mode).
5. CRM/Slack webhooks via Inngest (backlog crm-webhooks).

2026-06-10
----------
- Scaffolded TanStack Start + oRPC + Drizzle + Better Auth + Inngest.
- Postgres schema: forms, submissions, leads, analytics_events, auth tables.
- Admin: form list/create, JSON editor, publish, leads pipeline, basic analytics.
- Public multi-step forms at /f/:slug with field-level event tracking.
- Local Postgres database `formos` created (no Docker); schema pushed via drizzle-kit.
- First admin account created manually; public sign-up removed from UI and API.
- Code review (Bugbot + Security): 7 bug findings, 6 security findings recorded in context/review-findings.json.
- Review remediation: server-side validation, rate limits, DB transactions, aggregated stats, slugify fallback, analytics dedup, leads query fix, env hardening, OpenAPI auth in prod.
- Design system aligned with Everest Finance tokens (new_lp feat/pm-storytelling-refonte): mauve (#461D4C) + everest-green + gold; semantic token split fixed.
- UI revamp: admin sidebar, double-bezel Panel components, PageHeader/EmptyState, leads pipeline filters, form editor stat strip, public form hero + progress bar.

Stack
-----
Runtime: Bun
Framework: TanStack Start 1.x
API: oRPC (/api/rpc, OpenAPI at /api)
DB: PostgreSQL (local / Neon-ready)
Auth: Better Auth (email/password, sign-up disabled)
Jobs: Inngest (form/submission.completed)

- Auth redirect fix: sign-out now navigates to /login and invalidates router state; admin layout watches session reactively for loss/expiry.
- Audit pass: identified 11 bugs, 8 UX gaps, and 5 missing features (see backlog additions).

Next up (see context/backlog.json)
----------------------------------
- Visual form builder
- Embeds for marketing pages
- Webhook integrations for leads
